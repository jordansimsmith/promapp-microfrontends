{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "dev": "lerna run --parallel --stream dev",
    "start": "lerna run --parallel --stream start"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
